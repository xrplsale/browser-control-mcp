{
  "name": "browser-control-mcp",
  "version": "1.3.1",
  "private": true,
  "scripts": {
    "build": "nx run-many --target=build --all --parallel"
  },
  "devDependencies": {
    "nx": "20.6.0"
  }
}